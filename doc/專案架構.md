WeddingFront/
├── package.json               # Nuxt 3 + GSAP + google-fonts + sass
├── nuxt.config.ts             # 模組設定、SCSS global inject、images 靜態資源
├── tsconfig.json
├── .env.example               # API_BASE_URL
├── wedding.config.ts          # ⭐ 所有可修改資料（名字/日期/地點/照片分配）
│
├── app.vue                    # 入口
├── pages/index.vue            # 組合所有 section
├── plugins/gsap.client.ts     # 全域注入 $gsap + $ScrollTrigger
│
├── components/
│   ├── sections/
│   │   ├── HeroSection.vue     # 滿版背景 + Ken Burns + 名字 + SweetDay
│   │   ├── StorySection.vue    # 相識/交往/求婚 滾動故事
│   │   ├── EmotionSection.vue  # 情緒高潮 + 視差 + LoveYou
│   │   ├── InfoSection.vue     # 婚禮日期/時間/地點卡片
│   │   ├── GallerySection.vue  # 32 張照片 grid + lightbox
│   │   ├── RsvpSection.vue     # 完整表單（含條件顯示地址）
│   │   └── FooterSection.vue   # 海邊照 + Thanks 簽名
│   └── common/
│       ├── MusicPlayer.vue     # 固定浮動播放鈕
│       └── ImageLightbox.vue   # 全螢幕相片預覽
│
├── composables/
│   ├── useGsap.ts             # GSAP helper（animateFadeUp, scrollReveal）
│   ├── useRsvp.ts             # 表單狀態 + submit → POST /rsvp
│   └── useGallery.ts          # 相簿狀態 + open/close lightbox
│
├── assets/scss/
│   ├── _variables.scss        # 色盤、字體、間距、斷點變數
│   ├── _mixins.scss           # RWD mixin、版面 mixin
│   ├── _reset.scss            # Modern CSS reset
│   ├── _typography.scss       # .t-display、.t-heading 等 utility class
│   └── main.scss              # :root CSS 自訂屬性、全域 film grain
│
├── utils/api.ts               # createApi() factory（POST /rsvp 等）
└── types/index.ts             # RsvpForm、WeddingInfo、GalleryImage 型別


開始前先做：

編輯 wedding.config.ts — 修改新人名字、日期、地點
把婚禮背景音樂放到 public/music/wedding-bgm.mp3
調整照片分配編號（photos.hero、photos.storyMeet 等）
複製 .env.example 為 .env，填入 Laravel API 位址